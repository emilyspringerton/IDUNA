# VS8 — Auraborialis Reboot Protocol (STATUS: not-yet-built; doctrine reincarnated in SHANKPIT)

`supersedes: vs8.md` (archived at `docs/archive/kikoryu-vs-original/vs8.md`)

*2026-07-16. See `docs/VS_REALITY_AUDIT.md` §VS8.*

VS8 specified cyclical world resets that never erase history: permanent
`cycle_id`s, identity/entitlements/roles persisting across the boundary,
world state re-projected into a new namespace, prior cycles queryable
forever, reset as a constitutional multi-agent event.

**Nothing of it exists in IDUNA** (verified: no cycle/epoch/continuity
identifiers in any schema or handler). The doctrine was independently
restated as SHANKPIT's **season lineage** (`SHANKPIT/docs2/NORTHSTAR.md`
Layer 4: worlds reboot but "do not start from zero" — snapshots, lineage
chains, visible scars) — doc-level there too, unbuilt in both places, and
with no citation between them.

**Disposition:** archived as a world-engine protocol — full Auraborialis
belongs to whatever persistent world (DragonsNShit/SHANKPIT) eventually needs
it, not to IDUNA. One clause is adopted into the tournaments platform now:
**seasons are miniature cycles.** VS10 contexts carry a first-class
`season_id` from v0, season close is an explicit archival event, archived
seasons remain permanently queryable, and identity/reputation (VS0/VS9)
persist across season boundaries. If a full reset protocol is ever needed,
that seam is where it attaches. Revisit only if the founder reopens it.
